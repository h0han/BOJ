#include <stdio.h>

int main()
{
	int st1, st2, st3, st4, st5;
	scanf("%d\n%d\n%d\n%d\n%d", &st1, &st2, &st3, &st4, &st5);
	int arr[5] = [st1, st2, st3, st4, st5];
	int sum = st1 + st2 + st3 + st4 + st5;
	while(i == 5){
		if (arr[i] < 40)
			arr[i] = 40;
			i++		
	}
	printf("%d", sum/5);
}
